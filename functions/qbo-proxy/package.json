{
  "name": "aaa-qbo-proxy",
  "version": "1.0.0",
  "description": "Server-side QuickBooks Online OAuth + Accounting API proxy for AAA HyperKernel. Holds the client secret and tokens; the browser never sees them.",
  "main": "index.js",
  "engines": { "node": "20" },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions:qboProxy",
    "test": "node test.js"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.0"
  }
}
