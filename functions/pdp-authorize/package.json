{
  "name": "aaa-pdp-authorize",
  "version": "1.0.0",
  "description": "Policy Decision Point thin shell for AAA HyperKernel (ATLAS Domain 1 keystone). Verifies Firebase ID tokens, resolves server-set custom claims, and runs the canonical artifact-driven decision core with shadow/enforce rollout modes.",
  "main": "index.js",
  "engines": { "node": "20" },
  "scripts": {
    "deploy": "firebase deploy --only functions:pdpAuthorize",
    "test": "node test.js"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.0"
  }
}
