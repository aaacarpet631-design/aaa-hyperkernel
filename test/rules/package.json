{
  "name": "aaa-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "description": "Firestore security-rules tests (run against the emulator).",
  "scripts": {
    "test": "node rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.0",
    "firebase-tools": "^13.0.0"
  }
}
