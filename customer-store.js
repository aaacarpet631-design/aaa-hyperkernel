// customer-store.js placeholder for AAA HyperKernel
;(function(global){
  'use strict';
  const store = {
    async boot() {
      // no-op
    }
  };
  global.AAA_CUSTOMER_STORE = store;
})(typeof window !== 'undefined' ? window : this);
